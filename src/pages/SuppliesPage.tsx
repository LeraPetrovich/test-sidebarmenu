import { type FC } from "react";
import { InfoPageText } from "../components";

export const SuppliesPage: FC = () => {
  return (
    <div className="flex items-center justify-center flex-1">
      <InfoPageText text="Supplies Page" />
    </div>
  );
};
