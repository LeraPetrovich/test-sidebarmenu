import { type FC } from "react";
import { InfoPageText } from "../components";

export const NotificationPage: FC = () => {
  return (
    <div className="flex items-center justify-center flex-1">
      <InfoPageText text="Notification Page" />
    </div>
  );
};
